'use client';
import { type ReactElement } from 'react';

import RegisterPage from '@/components/templates/RegisterPage';

const Register = (): ReactElement => <RegisterPage />;

export default Register;
