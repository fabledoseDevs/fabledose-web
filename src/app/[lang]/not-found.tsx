import type { ReactElement } from 'react';

import NotFoundTemplate from '@/components/templates/NotFound';

const NotFoundPage = (): ReactElement => <NotFoundTemplate />;

export default NotFoundPage;
